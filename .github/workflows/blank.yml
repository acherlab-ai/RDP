name: Test Depot

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: depot-ubuntu-24.04-8

    steps:
      - uses: actions/checkout@v4

      - name: Check hardware
        run: |
          echo "CPU:"
          nproc
          echo "RAM:"
          free -h
          echo "DISK:"
          df -h
